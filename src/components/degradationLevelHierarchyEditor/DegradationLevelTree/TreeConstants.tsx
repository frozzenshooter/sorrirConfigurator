
export const DEFAULT_NODE_WIDTH: number = 200;
export const DEFAULT_NODE_HEIGHT: number = 64;


// global "left" offset of the tree itself
export const DEFAULT_TREE_X_OFFSET: number = 0;

// global "top" offset of the tree itself
export const DEFAULT_TREE_Y_OFFSET: number = 0;

export const DEFAULT_NODE_X_PADDING: number = 25;
export const DEFAULT_NODE_Y_PADDING: number = 50;